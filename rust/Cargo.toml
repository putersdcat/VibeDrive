[package]
name = "vibedrive-core"
version = "0.1.0"
edition = "2021"
description = "Particle + road sim core for VibeDrive (WASM)"
license = "MIT"
publish = false

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
